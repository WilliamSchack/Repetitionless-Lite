Check which textures are assigned
Check which textures assign to what index

add to one integer
- first part which textures assigned
- second part which index each texture is assigned to

----
can be stored in only which textures are in what index
if nonstored textures have -1 value and the rest a value
that will work